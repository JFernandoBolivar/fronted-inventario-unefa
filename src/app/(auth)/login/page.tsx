import FormLogin from "@/components/form-login";

const LoginPage = () => {
  return (
    <div className="bg-[url(/fondo.png)] w-full h-screen flex items-center justify-center bg-cover">
      <FormLogin />
    </div>
  );
};

export default LoginPage;
